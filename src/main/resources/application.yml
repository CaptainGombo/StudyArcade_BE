spring:
  profiles:
    include: secret
  thymeleaf:
    cache: 'false'

  redis:
    host: localhost
    port: 6379
  jpa:
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: true
        format_sql: true
    open-in-view: true
    hibernate:
      ddl-auto: update

